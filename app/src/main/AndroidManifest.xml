<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.proyecto1">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".oceavapagina"></activity>
        <activity android:name=".decimapagina" />
        <activity android:name=".novenaPagina" />
        <activity android:name=".octavaPagina" />
        <activity android:name=".septimaPagina" />
        <activity android:name=".sextaPagina" />
        <activity android:name=".quintaPagina" />
        <activity android:name=".cuartaPagina" />
        <activity android:name=".terceraPagina" />
        <activity android:name=".segundaPagina" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>